# PostgreSQL URL
DATABASE_URL=

# Or use individual vars (used when DATABASE_URL is empty)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASS=postgres
DB_NAME=timetable

# HTTP server port
PORT=8080
